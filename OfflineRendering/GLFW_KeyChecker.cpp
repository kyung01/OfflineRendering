#include "GLFW_KeyChecker.h"

void GLFW_KeyChecker::checkKey(int key, int keyState)
{
	for (std::map<int,int>::iterator it = mapKeyState.begin(); it != mapKeyState.end(); it++) {
		//is key pressed d
	}
}
